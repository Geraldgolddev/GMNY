# ─────────────────────────────────────────────────────────────
# NairaFlow — Root environment template
# Copy to `.env` and adjust. Never commit real secrets.
# ─────────────────────────────────────────────────────────────

# Runtime
NODE_ENV=development

# ── Database (PostgreSQL) ────────────────────────────────────
DATABASE_URL="postgresql://nairaflow:nairaflow@localhost:5432/nairaflow?schema=public"
# Used by the e2e test suite
DATABASE_URL_TEST="postgresql://nairaflow:nairaflow@localhost:5432/nairaflow_test?schema=public"

# ── Redis (queues / cache) ───────────────────────────────────
REDIS_URL="redis://localhost:6379"

# ── API ──────────────────────────────────────────────────────
API_PORT=4000
API_GLOBAL_PREFIX=api
API_CORS_ORIGINS="http://localhost:3000,http://localhost:3001"

# ── Auth / JWT ───────────────────────────────────────────────
# Generate with: openssl rand -hex 48
JWT_ACCESS_SECRET="dev-access-secret-change-me-000000000000000000000000000000"
JWT_REFRESH_SECRET="dev-refresh-secret-change-me-11111111111111111111111111111111"
JWT_ACCESS_TTL="15m"
JWT_REFRESH_TTL="7d"
BCRYPT_SALT_ROUNDS=12

# ── Blockchain / Circle (Base + USDC) ────────────────────────
CIRCLE_API_KEY=""
CIRCLE_API_BASE_URL="https://api-sandbox.circle.com"
BASE_RPC_URL="https://sepolia.base.org"
USDC_CONTRACT_ADDRESS="0x036CbD53842c5426634e7929541eC2318f3dCF7e"

# ── Web / Admin frontends ────────────────────────────────────
NEXT_PUBLIC_API_URL="http://localhost:4000/api"
WEB_PORT=3000
ADMIN_PORT=3001
